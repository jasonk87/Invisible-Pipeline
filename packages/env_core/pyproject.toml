[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "env_core"
version = "0.1.0"
description = "Shared module-owned environment loader"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["env_core*"]
