[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "llm_core"
version = "0.1.0"
description = "Minimal object-oriented LLM call layer"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["llm_core*"]
